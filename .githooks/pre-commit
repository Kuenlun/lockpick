#!/usr/bin/env bash
set -euo pipefail

echo "Running lockpick pre-commit check..."

if ! ./lockpick; then
  echo ""
  echo "ERROR: lockpick failed. Commit aborted."
  echo "Fix the reported issues and run ./lockpick successfully before committing."
  exit 1
fi

echo "lockpick passed. Proceeding with commit."
